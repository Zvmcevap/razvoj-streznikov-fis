syntax = "proto3";

package chat;

message Empty {}

message Note {
  string name = 1;
  string message = 2;
}

service ChatServer {
  rpc SendNote(Note) returns (Empty);
  rpc ChatStream(Empty) returns (stream Note);
}